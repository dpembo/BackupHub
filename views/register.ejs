<!DOCTYPE html>
<html>
<head>
  <title>BackupHub | Login</title>
  <link rel="stylesheet" href="css/materialize/materialize.css">
  <link id="maincss" rel="stylesheet" href="css/light.css">
  <style>
    body {
        
      /*background-color: #f5f5f5;*/
      background: linear-gradient(to bottom right, #3498db, #e74c3c);
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      margin: 0;
    }
    .card-panel {
      width: 360px;
      padding: 20px;
      border-radius: 10px;
      box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
    }
    h1 {
      font-size: 24px;
      margin-bottom: 20px;
      text-align: center;
      color: #333;
    }
    label {
      font-size: 14px;
      color: #666;
    }
    input[type="text"],
    input[type="password"] {
      border: none;
      border-bottom: 1px solid #ccc;
      outline: none;
      width: 100%;
      padding: 5px 0;
      font-size: 16px;
      margin-bottom: 20px;
      color: #333;
    }
    .btn {
      background-color: #4caf50;
      border: none;
      border-radius: 5px;
      padding: 10px 20px;
      font-size: 16px;
      color: #fff;
      cursor: pointer;
      transition: background-color 0.2s;
    }
    .btn:hover {
      background-color: #45a049;
    }
    .forgot-password {
      text-align: center;
      margin-top: 20px;
      font-size: 14px;
      color: #666;
    }

    #vNo
    {
      position: relative;
      top: 242px;
      float: right;
      color: gray;
    }

    .imgframe {
      display: block;
      text-align:center;
    }

    
  </style>
</head>
<body>
  <div class="card-panel">
    <span class="imgframe"><img src="/images/backupcontrol-logo.png" style="width:210px;"></span>
    <h1>BackupHub Register</h1>
    <p>No user is regiestered.<br>Please create a user profile for access</p>
    <form class="col s12" action="/register.html" method="POST">
      <input type="hidden" name="_csrf" value="<%= csrf %>">
      <label for="username">Username</label>
      <input id="username" type="text" name="username" required>
      <label for="email">Email</label>
      <input id="email" type="email" name="email" required>
      <label for="password">Password</label>
      <input id="password" type="password" name="password" required>
      <button class="btn waves-effect waves-light" style="line-height:0px" type="submit">Register</button>
    </form>
  </div>
  <script src="js/materialize/materialize.js"></script>
  <script>
    // Check if the 'message' URL parameter is present
    const urlParams = new URLSearchParams(window.location.search);
    const message = urlParams.get('message');
    
    if (message) {
      // Show a Materialize toast notification with the provided message
      M.toast({html: message, displayLength: 5000});
    }
  </script>
</body>
</html>