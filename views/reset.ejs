<!DOCTYPE html>
<html>
<head>
  <title>BackupHub | Login</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css">
  <style>
    body {
        
      /*background-color: #f5f5f5;*/
      background: linear-gradient(to bottom right, #3498db, #e74c3c);
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      margin: 0;
    }
    .card-panel {
      width: 360px;
      padding: 20px;
      border-radius: 10px;
      box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
    }
    h1 {
      font-size: 24px;
      margin-bottom: 20px;
      text-align: center;
      color: #333;
    }
    label {
      font-size: 14px;
      color: #666;
    }
    input[type="text"],
    input[type="password"] {
      border: none;
      border-bottom: 1px solid #ccc;
      outline: none;
      width: 100%;
      padding: 5px 0;
      font-size: 16px;
      margin-bottom: 20px;
      color: #333;
    }
    .btn {
      background-color: #4caf50;
      border: none;
      border-radius: 5px;
      padding: 10px 20px;
      font-size: 16px;
      color: #fff;
      cursor: pointer;
      transition: background-color 0.2s;
    }
    .btn:hover {
      background-color: #45a049;
    }
    .forgot-password {
      text-align: center;
      margin-top: 20px;
      font-size: 14px;
      color: #666;
    }
  </style>
</head>
<body>
  <div class="card-panel">
    <span class="imgframe"><span class="loginimg"></span></span>
    <h1>Reset Password</h1>
    <p>Please provide a new password to reset your account, and then press 'Reset Password'</p>
    <form class="col s12" action="/reset/<%= token %>/<%=user%>" method="POST">
      <label for="username">New Password</label>
      <input id="password" type="password" name="password" required>
      <button class="btn waves-effect waves-light orange" style="line-height:0px" type="submit">Reset Password</button>
    </form>
    <p class="forgot-password"><a href="/login.html">Login</a></p>
  </div>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/js/materialize.min.js"></script>
  <script>
    // Check if the 'message' URL parameter is present
    const urlParams = new URLSearchParams(window.location.search);
    const message = urlParams.get('message');
    
    if (message) {
      // Show a Materialize toast notification with the provided message
      M.toast({html: message, displayLength: 5000});
    }
  </script>
</body>
</html>